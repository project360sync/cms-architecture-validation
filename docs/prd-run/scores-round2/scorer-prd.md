# Round 2 — Blind scorer B (PRD-specific metrics) — verbatim output
Fresh opus instance (aaa208a91941dc536), 2026-07-24; round-1 material off-limits. Verified the committed test log (176 tests at pinned commit 0b56a13).

## FALSIFIABILITY — SCORE: 9/10
- A1 is a textbook exact negation of Gate A, window-bounded: Gate A = (≥2 fizető) AND (≥1 dokumentált off-the-shelf-kudarc); A1 = "kevesebb mint 2 fizető átadás VAGY nulla dokumentált, releváns off-the-shelf-kudarc… (D5, zárás 2026-11-30)" — precisely De Morgan's P<2 OR F=0, and it spells out that 2 payers without a failure also kills the custom-architecture thesis. Verified against §9 and D1.
- Gate B threshold numeric, observable, tightly scoped (mount/refresh/destroy + ≥2× length on the fixed matrix, no manual retuning) with explicit scope disclaimer relocating §15.3/(A) to Phase-2 exit. "'Úgy tűnik, működik' nem elég."
- Gate C observable and closes exactly the promo caveat (measured delivery cost at non-promo price + ≥1 non-promo Business offer confirmed). Promo caveat consistent across §4/§9/§10/§11.
- Interest never counted as evidence.
missing for 10: A4's "lényegesen 70 fölött" is soft — tighten to e.g. ">70h measured on the first two reuse-eligible Business builds". A3's "gond nélkül kiszolgál" qualitative but demonstrable.

## HONESTY — SCORE: 9/10
- §4 better-served table keeps the self-undermining rows verbatim-faithfully, incl. "Ritka szerkesztés → Ne épüljön CMS"; incumbents named with redirect triggers [ARCH §12.2/§12.4].
- Sharpest limit with consequence: "a mitigáció szűkíti a megkülönböztetőt — átfed a Webflow/Framer natív képességeivel; …a kereslet-kapunak BIZONYÍTANIA kell, nem feltételeznie" [ARCH §16.6] — the PRD losing an argument with itself.
- Non-goals each name real alternatives; failure pivots concede competitors and possible shutdown.
- Evidence honesty: Gate B concedes GSAP feasibility is capability-level, not the literal vibor page [CMS note].
missing for 10: some §4 redirect triggers restate the need rather than give an in-conversation switch-signal; §3 non-goal triggers live in the §4 table rather than inline. No strawmen.

## METRIC QUALITY — SCORE: 9/10
- Primary behavioral + instrumented ("mérni, nem kérdezni").
- Guardrail measurable against a DEFINED baseline: pre-self-edit onboarding window per client, own-baseline comparison, >10% investigate / sustained = violation; honestly flags "ma nincs adat — a baseline-mérés a Gate A része".
- Role/Timing separate; baselines on every row; post-gate trajectory exists via the MRR supporting row (~216k HUF/yr, 5th-client portfolio ~1M [GTM §3.5]).
- The guardrail genuinely guards the primary (hand-held "edits" raise support-hours) — the rubric's 10-marker.
missing for 10: primary Target identical to the Gate A threshold; no numeric sustained edit-frequency target (median self-edits/client/month over N months); GAP-A6 acknowledged but not converted to a number.

SUMMARY: F=9 H=9 MQ=9
